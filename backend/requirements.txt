fastapi
uvicorn[standard]

# Data validation
pydantic

# File handling / utilities
python-multipart

# Scheduling (for task reminders)
apscheduler

# Optional (if you're using environment variables)
python-dotenv

# Optional (if you add a database later)
sqlalchemy
psycopg2-binary

# Optional (if you switch to MongoDB)
pymongo
